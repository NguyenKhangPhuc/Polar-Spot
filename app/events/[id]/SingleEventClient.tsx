/**
 * PURPOSE:
 * Re-exports the refactored SingleEventClient component from the components subfolder.
 *
 * CONTEXT/PARENT FILE:
 * Extracted into app/events/[id]/components/SingleEventClient.tsx to decompose monolithic UI components.
 *
 * INPUTS / PARAMETERS:
 * - See app/events/[id]/components/SingleEventClient.tsx for full interface specifications.
 */

export { default, SingleEventClient } from "./components/SingleEventClient";
